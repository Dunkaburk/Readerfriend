# Baked into production builds (`vite build`) — the deployed Worker API.
# `vite dev` does not read this file; dev keeps the localhost:8787 default.
# Never put tokens here — paste the token in Settings (localStorage).
VITE_API_BASE=https://readerfriend-api.dunkaburk.workers.dev
