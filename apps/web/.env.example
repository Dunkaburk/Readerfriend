# Base URL of the Worker API (wrangler dev serves http://localhost:8787).
VITE_API_BASE=http://localhost:8787

# Shared bearer token. Convenience default only — the token is normally pasted
# into Settings (stored in localStorage). Leave empty and paste it in the app.
VITE_APP_TOKEN=
